<div class="message-container" id="message-<%= message.id %>">
  <div class=" <%= 'me' if message.user == current_user %> ">
    <i class="author">
      <span><%= message.user.first_name %></span>
      <small><%= message.created_at.strftime("%a %b %e at %l:%M%p") %></small>
    </i>
    <p><%= message.content %></p>
  </div>
</div>
